import { useDidUpdateEffect } from '@modern-kit/react'
import { useState } from 'react'

# useDidUpdateEffect

A custom hook that skips execution on the initial mount and only runs the effect when the dependency array (`deps`) values are updated.

<br />

## Code
[🔗 View source code](https://github.com/modern-agile-team/modern-kit/blob/main/packages/react/src/hooks/useDidUpdateEffect/index.ts)

<br />

## Interface
```ts title="typescript"
function useDidUpdateEffect(effect: EffectCallback, deps: DependencyList): void
```

<br />

### Parameters

| Name | Type | Description |
| ---- | ---- | ----------- |
| `effectCallback` | `EffectCallback` | The effect function to execute |
| `deps` | `DependencyList` | Dependency array that re-runs the effect when changed |

<br />

## Usage
```tsx title="typescript"
import { useDidUpdateEffect } from '@modern-kit/react'

const Example = () => {
  const [count, setCount] = useState(0);
  const [isUpdated, setIsUpdated] = useState(false);

  useDidUpdateEffect(() => {
    setIsUpdated(true);
  }, [count]);


  return (
    <div>
      <button onClick={() => setCount(count + 1)}>Increment: {count}</button>
      {isUpdated && <p>count has been updated. {count}</p>}
    </div>
  );
}
```

## Example

export const Example = () => {
  const [count, setCount] = useState(0);
  const [isUpdated, setIsUpdated] = useState(false);

  useDidUpdateEffect(() => {
    setIsUpdated(true);
  }, [count]);

  return (
    <div style={{ display: 'flex', flexDirection: 'column', gap: '12px', padding: '16px', border: '1px solid #e0e0e0', borderRadius: '8px' }}>
      <div style={{ display: 'flex', gap: '8px', alignItems: 'center' }}>
        <button onClick={() => setCount(count + 1)}>Increment: {count}</button>
      </div>
      {isUpdated && <p>count has been updated. {count}</p>}
    </div>
  );
}

<Example />
