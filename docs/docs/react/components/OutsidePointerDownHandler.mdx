import { useState } from 'react';
import { OutsidePointerDownHandler } from '@modern-kit/react';
import BrowserOnly from '@docusaurus/BrowserOnly';

# OutsidePointerDownHandler

`useOutsidePointerDown`의 기능을 선언적으로 처리하는 컴포넌트입니다.

`OutsidePointerDown`로 감싸진 컴포넌트 외부를 클릭하는 경우 `callback`이 실행됩니다.

`as` 속성을 통해 OutsidePointerDown 컴포넌트의 원하는 태그로 변경 할 수 있습니다. (아래 예제를 참고해주세요)

<br />

## Code
[🔗 실제 구현 코드 확인](https://github.com/modern-agile-team/modern-kit/blob/main/packages/react/src/components/OutsidePointerDownHandler/index.tsx)

## Interface
```ts title="typescript"
interface OutsidePointerDownHandlerProps {
  asChild?: boolean;
  onPointerDown: () => void;
  children: ReactNode;
}
```
```ts title="typescript"
const OutsidePointerDown: PolyForwardComponent<
  'div',
  OutsidePointerDownProps,
  React.ElementType
>;
```

## Usage
### Default
```tsx title="typescript"
import { OutsidePointerDown } from '@modern-kit/react';

const DefaultExample = () => {
  const style = {
    width: '500px',
    height: '100px',
    backgroundColor: 'skyBlue',
  };
  return (
    <OutsidePointerDownHandler
      style={style}
      callback={() => console.log('DefaultExample Outside Clicked!')}>
      외부 영역 클릭 후 콘솔을 확인해주세요.
    </OutsidePointerDownHandler>
  );
};
```

export const DefaultExample = () => {
  const style = {
    width: '500px',
    height: '100px',
    backgroundColor: 'skyBlue',
  };
  return (
    <OutsidePointerDownHandler
      style={style}
      callback={() => console.log('DefaultExample Outside Clicked!')}>
      외부 영역 클릭 후 콘솔을 확인해주세요.
    </OutsidePointerDownHandler>
  );
};


<BrowserOnly>
  {() => <DefaultExample />}
</BrowserOnly>

<br />

### As-Example
```tsx title="typescript"
import { OutsidePointerDownHandler } from '@modern-kit/react';

const AsExample = () => {
  const style = {
    width: "300px",
    height: "50px",
  };
  return (
    // OutsidePointerDownHandler 컴포넌트의 "제네릭 타입" 지정 후 "as" props를 입력해주세요. 
    // 원활한 타입 추론이 가능합니다.
    <OutsidePointerDownHandler<'input'> 
      as="input"
      placeholder="외부 영역 클릭 후 콘솔을 확인해주세요."
      callback={() => console.log('As-Example Outslide Clicked!')}
      style={style}
    />
  );
};
```

export const AsExample = () => {
  const style = {
    width: "300px",
    height: "50px",
  };
  return (
    <OutsidePointerDownHandler
      as="input"
      placeholder="외부 영역 클릭 후 콘솔을 확인해주세요."
      callback={() => console.log('As-Example Outslide Clicked!')}
      style={style}
    />
  );
};

<BrowserOnly>
  {() => <AsExample />}
</BrowserOnly>