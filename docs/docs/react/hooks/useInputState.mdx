import { useInputState } from '@modern-kit/react'

# useInputState

input 필드의 상태를 관리하는 커스텀 훅 입니다.

<br />

## Code
[🔗 실제 구현 코드 확인](https://github.com/modern-agile-team/modern-kit/blob/main/packages/react/src/hooks/useInputState/index.ts)

## Interface
```ts title="typescript"
interface UseInputStateOptions {
  validate?: (value: string) => string | null | undefined;
}

interface UseInputStateReturn {
  value: string;
  error: string | null;
  onChange: (e: ChangeEvent<HTMLInputElement>) => void;
  resetError: () => void;
  resetValue: () => void;
  reset: () => void;
}
```
```ts title="typescript
function useInputState(
  initialValue?: string,
  options?: UseInputStateOptions
): UseInputStateReturn;
```

## Usage
```tsx title="typescript"
import { useInputState } from '@modern-kit/react';

const Example = () => {
  const { value, error, onChange, reset } = useInputState('', {
    validate: (value) => value.length > 10 ? 'value는 10자 이상이면 안됩니다.' : undefined
  });

  return (
    <>
      <h1>{value}</h1>
      <p>{error}</p>
      <input type="text" name="test" onChange={onChange} value={value} />
      <button onClick={reset}>reset</button>
    </>
  );
};
```

## Example

export const ExampleSingleInput = () => {
  const { value, error, onChange, reset } = useInputState('', {
    validate: (value) => value.length > 10 ? 'value는 10자 이상이면 안됩니다.' : undefined
  });

  return (
    <>
      <h1>test: {value}</h1>
      <p>error: {error}</p>
      <input type="text" name="test" onChange={onChange} value={value} /><br />
      <button onClick={reset}>reset</button>
    </>
  );
};

<ExampleSingleInput />